<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Walmart </title>
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Icons"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Icons+Outlined"
      rel="stylesheet"
    />
    <script src="https://use.fontawesome.com/4611f582cf.js"></script>
    <link rel="stylesheet" href="../css/base.css" />
    <link rel="stylesheet" href="../css/main.css" />
    <link rel="stylesheet" href="../css/login&register.css" />
    <script src="https://apis.google.com/js/api:client.js"></script>
    <style>
      .invalid-info-message {
        font-size: 15px;
        color: #f33a58;
        display: block;
        margin-left: 12px;
        text-align: left;
        margin-top: -20px;
        display: none;
      }

      #form h3 {
        margin-bottom: 20px;
      }
      #form .form-group {
        margin-top: 10px;
        margin-bottom: 0;
      }
      .form-group .form-message {
        margin-top: -20px;
        display: none;
      }
      .social {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 10px;
      }
      .google_btn,
      .facebook_btn {
        width: 50%;
        gap: 5px;
      }
      .google_btn {
        background-color: #e64839;
      }
      .other .forgot_password {
        color: red;
      }
    </style>
  </head>
  <body>
    <div class="container">      
      <div class="wrap">
        <form id="form" method="POST">
          <h3 class="txt">ĐĂNG NHẬP</h3>
          <div class="form-group">
            <div class="wrap_input">
              <input
                type="text"
                class="email_or_phone login_input"
                id="email_or_phone"
                placeholder="Nhập số điện thoại hoặc email"
                name="email_or_phone"
              />
              <span class="focus-input"></span>
            </div>
            <span class="form-message email_or_phone_message"
              >Số điện thoại hoặc email không được để trống</span
            >
            <span class="invalid-info-message"
              >Email hoặc mật khẩu không chính xác</span
            >
          </div>
          <div class="form-group">
            <div class="wrap_input">
              <input
                type="password"
                class="password login_input"
                id="password"
                placeholder="Nhập mật khẩu"
                name="password"
              />
              <span class="focus-input"></span>
            </div>
            <span class="form-message password_message"
              >Mật khẩu không được để trống</span
            >
            <span class="invalid-info-message"
              >Email hoặc mật khẩu không chính xác</span
            >
          </div>
          <button id="login" type="button" class="btn form_btn txt">
            ĐĂNG NHẬP
          </button>          
          <div class="other txt">
            <span>Bạn chưa có tài khoản?</span>
            <a href="/register" class="register">Đăng ký mới</a>
            <br />
            <a href="#" class="forgot_password">Quên mật khẩu?</a>
          </div>
        </form>
      </div>
    </div>
    <script src="../js/login.js"></script>
  </body>
</html>
